# K3s Server Connection Configuration
# REQUIRED: URL of your k3s server (including port, typically 6443)
# Example: https://k3s-server.example.com:6443
K3S_URL=https://your-k3s-server:6443

# REQUIRED: Agent token for joining the cluster
# Get this from your k3s server at: /var/lib/rancher/k3s/server/node-token
# Or run on server: sudo cat /var/lib/rancher/k3s/server/node-token
K3S_TOKEN=your-node-token-here

# Node Configuration
# Optional: Unique name for this agent node (defaults to k3s-agent-node1)
NODE_NAME=k3s-agent-node1

# Optional: Node labels for workload scheduling (comma-separated key=value pairs)
# Example: environment=production,region=us-west
NODE_LABELS=

# Optional: Additional k3s components to disable (comma-separated)
# Example: traefik,servicelb
# K3S_DISABLE=

# Optional: Cluster secret (if your cluster uses one)
# K3S_CLUSTER_SECRET=

K3S_NODE_IP=your-local-ip-address